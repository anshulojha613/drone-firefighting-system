# Drone Firefighting System - Dependencies
# Compatible with Python 3.11+ (tested on 3.13)

# For production deployment, use scenario-specific requirements:
# - requirements/ground_station.txt (Mac/Linux)
# - requirements/sd_drone.txt (Raspberry Pi - Scouter)
# - requirements/fire_drone.txt (Raspberry Pi - Firefighter)

# Database
sqlalchemy>=2.0.35
alembic>=1.13.1

# Web Framework & Dashboard
flask==3.0.0
flask-socketio==5.3.5
dash==2.14.2
dash-bootstrap-components==1.5.0
plotly==5.18.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.0

# Networking
requests==2.31.0
python-socketio==5.10.0

# Configuration
pyyaml==6.0.1
python-dateutil==2.8.2

# Drone Communication (for hardware mode)
pymavlink>=2.4.41
dronekit>=2.9.2
pyserial>=3.5
future>=1.0.0  # Required for dronekit Python 3.13 compatibility

# Image Processing
opencv-python>=4.8.0
pillow>=10.0.0

# ============================================
# HARDWARE SENSORS (Raspberry Pi)
# ============================================

# Thermal Camera (MLX90640)
adafruit-circuitpython-mlx90640>=1.3.0
adafruit-blinka>=8.20.0
adafruit-circuitpython-busdevice>=5.2.0
Adafruit-PlatformDetect>=3.40.0

# Environment Sensor (DHT22)
adafruit-circuitpython-dht>=3.7.0

# Note for Camera:
# Raspberry Pi camera uses rpicam-still command-line tool (pre-installed on Raspberry Pi OS)
# No additional Python packages needed for camera capture

# Note for macOS:
# Hardware sensors (thermal, DHT22) require GPIO access which is not available on Mac
# The code will run in simulation mode on Mac for testing purposes

# ============================================
# OPTIONAL: Machine Learning
# ============================================
# Uncomment if using ML-based fire detection (basic fire detection uses thermal thresholding)
# tensorflow==2.15.0
# tflite-runtime>=2.14.0  # Lightweight alternative for Raspberry Pi

# Geospatial
folium==0.15.1

# Utilities
pytz==2023.3
tzlocal==5.2

# Logging
colorlog==6.8.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0

# Data Visualization
matplotlib>=3.5.0
